# Halısaha Web

React/Vite web client for the Halısaha application.

## Requirements

- Node.js 18 or newer
- npm 9 or newer

## Setup

```sh
npm ci
cp .env.example .env
```

Set `VITE_API_BASE_URL` in `.env` to the backend base URL.

## Commands

```sh
npm run dev
npm run lint
npm run build
```
